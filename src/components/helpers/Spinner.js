import React from "react";

const Spinner = () => {
    return (
        <div className='spinner-loader-component-container'>
            <div className="spinner-border"/>
        </div>
    )
};

export default Spinner;